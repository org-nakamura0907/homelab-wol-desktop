name: CD (manual release)

on:
  workflow_dispatch: {}

jobs:
  release-macos:
    name: Release (macOS manual)
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - name: Run shared release steps
        uses: ./.github/actions/build-steps
        with:
          artifact-name: tauri-bundle-macos
          upload-artifact: "true"

  # release-linux:
  #   name: Release (Linux manual)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
  #     - name: Run shared release steps
  #       uses: ./.github/actions/build-steps
  #       with:
  #         artifact-name: tauri-bundle-linux

  # release-windows:
  #   name: Release (Windows manual)
  #   runs-on: windows-latest
  #   steps:
  #     - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
  #     - name: Run shared release steps
  #       uses: ./.github/actions/build-steps
  #       with:
  #         artifact-name: tauri-bundle-windows
